import Link from "next/link";
import { ScrollText } from "lucide-react";
import { redirect } from "next/navigation";
import { Suspense } from "react";

import { SalesOrderDetail } from "@/components/crm/sales-order-detail";
import { DataPagination } from "@/components/data-pagination";
import { DebouncedSearchInput } from "@/components/debounced-search-input";
import { FilterBarSkeleton, TableSkeleton } from "@/components/loading-skeletons";
import { PageHeader } from "@/components/page-header";
import { SortableTableHead } from "@/components/sortable-table-head";
import { SalesOrderStatusBadge } from "@/components/status-badge";
import { Button } from "@/components/ui/button";
import { Empty, EmptyDescription, EmptyHeader, EmptyMedia, EmptyTitle } from "@/components/ui/empty";
import { Field, FieldLabel } from "@/components/ui/field";
import { Input } from "@/components/ui/input";
import { NativeSelect, NativeSelectOption } from "@/components/ui/native-select";
import { Skeleton } from "@/components/ui/skeleton";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { getSalesOrders, type SalesOrderListSort, type SalesOrderListStatus, type SortDirection } from "@/lib/crm/data";
import { parseDocumentDateRange } from "@/lib/crm/document-list-filters";
import { formatCurrency, formatDate } from "@/lib/crm/format";
import { DATA_PAGE_SIZE, DATA_PAGE_SIZES, parsePageParam, parsePageSizeParam } from "@/lib/pagination";

type SearchParams = Promise<Record<string, string | string[] | undefined>>;
type TableState = { query: string; status: SalesOrderListStatus; from: string; to: string; page: number; pageSize: number; sort: SalesOrderListSort; direction: SortDirection };
const first = (value: string | string[] | undefined) => Array.isArray(value) ? value[0] : value;
const SORTS = ["salesOrderNo", "customer", "purchaseOrderNo", "invoiceNo", "status", "total", "acceptedAt"] as const satisfies readonly SalesOrderListSort[];
const DIRECTIONS = ["asc", "desc"] as const satisfies readonly SortDirection[];
const defaultDirection = (sort: SalesOrderListSort): SortDirection => sort === "acceptedAt" ? "desc" : "asc";
function tableHref(state: TableState, changes: Partial<TableState>) { const next = { ...state, ...changes }; const params = new URLSearchParams(); if (next.query) params.set("q", next.query); if (next.status !== "all") params.set("status", next.status); if (next.from) params.set("from", next.from); if (next.to) params.set("to", next.to); if (next.sort !== "acceptedAt") params.set("sort", next.sort); if (next.direction !== defaultDirection(next.sort)) params.set("order", next.direction); if (next.pageSize !== DATA_PAGE_SIZE) params.set("pageSize", String(next.pageSize)); if (next.page > 1) params.set("page", String(next.page)); const query = params.toString(); return query ? `/crm/sales-orders?${query}` : "/crm/sales-orders"; }
function sortHref(state: TableState, sort: SalesOrderListSort) { const direction = state.sort === sort ? state.direction === "asc" ? "desc" : "asc" : defaultDirection(sort); return tableHref(state, { sort, direction, page: 1 }); }

async function SalesOrdersTableSection({ searchParams }: { searchParams: SearchParams }) {
  const params = await searchParams;
  const query = (first(params.q) ?? "").trim().slice(0, 80);
  const rawStatus = first(params.status); const status: SalesOrderListStatus = rawStatus === "ACTIVE" || rawStatus === "CANCELLED" ? rawStatus : "all";
  const range = parseDocumentDateRange(params.from, params.to); const page = parsePageParam(params.page); const pageSize = parsePageSizeParam(params.pageSize);
  const sort = SORTS.find((item) => item === first(params.sort)) ?? "acceptedAt"; const direction = DIRECTIONS.find((item) => item === first(params.order)) ?? defaultDirection(sort);
  const state = { query, status, from: range.from, to: range.to, page, pageSize, sort, direction } satisfies TableState;
  const { items, total, pageCount } = await getSalesOrders({ query, status, start: range.start, end: range.end, page, pageSize, sort, direction });
  const persistent = { q: query || undefined, status: status === "all" ? undefined : status, from: range.from || undefined, to: range.to || undefined, sort: sort === "acceptedAt" ? undefined : sort, order: direction === defaultDirection(sort) ? undefined : direction, pageSize: pageSize === DATA_PAGE_SIZE ? undefined : String(pageSize) };
  if (page > pageCount) redirect(tableHref(state, { page: pageCount }));
  const hasFilters = Boolean(query || status !== "all" || range.start);
  return <section className="flex min-w-0 flex-col overflow-hidden rounded-lg border bg-card" aria-label="Daftar Sales Order"><div className="flex flex-col gap-3 border-b p-4 lg:flex-row lg:items-center lg:justify-between"><DebouncedSearchInput key={query} initialValue={query} pathname="/crm/sales-orders" params={persistent} placeholder="Cari no. SO, PO, invoice, atau customer..." ariaLabel="Cari Sales Order" className="sm:max-w-md" /><form action="/crm/sales-orders" className="flex flex-wrap items-end gap-2">{query ? <input type="hidden" name="q" value={query} /> : null}{pageSize !== DATA_PAGE_SIZE ? <input type="hidden" name="pageSize" value={pageSize} /> : null}{sort !== "acceptedAt" ? <input type="hidden" name="sort" value={sort} /> : null}{direction !== defaultDirection(sort) ? <input type="hidden" name="order" value={direction} /> : null}<Field className="w-40 gap-1"><FieldLabel htmlFor="so-status">Status</FieldLabel><NativeSelect id="so-status" name="status" defaultValue={status}><NativeSelectOption value="all">Semua status</NativeSelectOption><NativeSelectOption value="ACTIVE">Aktif</NativeSelectOption><NativeSelectOption value="CANCELLED">Dibatalkan</NativeSelectOption></NativeSelect></Field><Field className="w-40 gap-1"><FieldLabel htmlFor="so-from">Dari tanggal</FieldLabel><Input id="so-from" name="from" type="date" max={range.today} defaultValue={range.from} /></Field><Field className="w-40 gap-1"><FieldLabel htmlFor="so-to">Sampai tanggal</FieldLabel><Input id="so-to" name="to" type="date" max={range.today} defaultValue={range.to} /></Field><Button type="submit" variant="outline">Terapkan</Button><Button variant="secondary" nativeButton={false} render={<Link href="/crm/sales-orders" />}>Reset</Button><p className="ml-auto text-xs text-muted-foreground"><strong className="font-medium text-foreground">{total}</strong> Sales Order</p></form></div>{items.length ? <div className="flex min-h-112 flex-1 flex-col"><Table className="min-w-5xl" containerClassName="min-h-0 flex-1 overflow-auto"><TableHeader className="sticky top-0 bg-muted"><TableRow className="hover:bg-muted"><TableHead className="w-16 text-center">No</TableHead><SortableTableHead label="No. SO" href={sortHref(state, "salesOrderNo")} active={sort === "salesOrderNo"} direction={direction} /><SortableTableHead label="Customer" href={sortHref(state, "customer")} active={sort === "customer"} direction={direction} /><SortableTableHead label="No. PO" href={sortHref(state, "purchaseOrderNo")} active={sort === "purchaseOrderNo"} direction={direction} /><SortableTableHead label="No. Invoice" href={sortHref(state, "invoiceNo")} active={sort === "invoiceNo"} direction={direction} /><SortableTableHead label="Status" href={sortHref(state, "status")} active={sort === "status"} direction={direction} /><SortableTableHead label="Total" href={sortHref(state, "total")} active={sort === "total"} direction={direction} className="text-right" /><SortableTableHead label="Tanggal diterima" href={sortHref(state, "acceptedAt")} active={sort === "acceptedAt"} direction={direction} /></TableRow></TableHeader><TableBody>{items.map((item, index) => <SalesOrderDetail key={item.id} id={item.id}><TableCell className="text-center font-mono text-muted-foreground tabular-nums">{(page - 1) * pageSize + index + 1}</TableCell><TableCell className="font-mono">{item.salesOrderNo}</TableCell><TableCell>{item.snapshotCompanyName ?? item.snapshotCustomerName}</TableCell><TableCell className="font-mono">{item.purchaseOrderNo}</TableCell><TableCell className="font-mono">{item.invoiceNo}</TableCell><TableCell><SalesOrderStatusBadge status={item.status} /></TableCell><TableCell className="text-right tabular-nums">{formatCurrency(item.total)}</TableCell><TableCell>{formatDate(item.acceptedAt)}</TableCell></SalesOrderDetail>)}</TableBody></Table><DataPagination pathname="/crm/sales-orders" page={page} pageCount={pageCount} total={total} pageSize={pageSize} pageSizeOptions={DATA_PAGE_SIZES} params={persistent} className="border-t px-4 py-3" /></div> : <Empty><EmptyHeader><EmptyMedia variant="icon"><ScrollText aria-hidden="true" /></EmptyMedia><EmptyTitle>{hasFilters ? "Sales Order tidak ditemukan" : "Belum ada Sales Order"}</EmptyTitle><EmptyDescription>{hasFilters ? "Coba ubah kata kunci atau filter yang digunakan." : "Sales Order muncul setelah peluang menjadi Deal."}</EmptyDescription></EmptyHeader></Empty>}</section>;
}

export default function SalesOrdersPage({ searchParams }: { searchParams: SearchParams }) { return <><PageHeader title="Sales Order" description="Pantau Sales Order yang terbentuk dari Deal." /><Suspense fallback={<section className="overflow-hidden rounded-lg border bg-card"><FilterBarSkeleton searchWidth="w-full sm:max-w-md" actionWidth="w-36" controls={5} /><TableSkeleton columns={8} rows={8} className="min-w-5xl" /><div className="flex justify-between gap-4 border-t px-4 py-3"><Skeleton className="h-4 w-44" /><Skeleton className="h-8 w-52" /></div></section>}><SalesOrdersTableSection searchParams={searchParams} /></Suspense></>; }
