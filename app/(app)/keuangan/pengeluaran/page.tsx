import Link from "next/link";
import { Check, ReceiptText, Trash2 } from "lucide-react";

import { deleteExpenseAction, reimburseExpenseAction } from "@/app/actions/finance";
import { ConfirmSubmitButton } from "@/components/confirm-submit-button";
import { DataPagination } from "@/components/data-pagination";
import { DebouncedSearchInput } from "@/components/debounced-search-input";
import { ExpenseForm } from "@/components/finance/expense-form";
import { PageHeader } from "@/components/page-header";
import { Button } from "@/components/ui/button";
import { Empty, EmptyDescription, EmptyHeader, EmptyMedia, EmptyTitle } from "@/components/ui/empty";
import { Field, FieldLabel } from "@/components/ui/field";
import { Input } from "@/components/ui/input";
import { NativeSelect, NativeSelectOption } from "@/components/ui/native-select";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { formatCurrency, formatDate } from "@/lib/crm/format";
import { getExpenses } from "@/lib/finance/expense";
import { EXPENSE_METHOD_LABEL, EXPENSE_METHODS } from "@/lib/finance/expense-methods";
import { parseFinanceDateRange } from "@/lib/finance/date-range";
import { getCurrentActor } from "@/lib/auth/session";
import { DATA_PAGE_SIZE, DATA_PAGE_SIZES, parsePageParam, parsePageSizeParam } from "@/lib/pagination";

type SearchParams = Promise<{ q?: string | string[]; from?: string | string[]; to?: string | string[]; method?: string | string[]; creator?: string | string[]; page?: string | string[]; pageSize?: string | string[] }>;
const first = (value: string | string[] | undefined) => Array.isArray(value) ? value[0] : value;

export default async function ExpensesPage({ searchParams }: { searchParams: SearchParams }) {
  const params = await searchParams; const query = (first(params.q) ?? "").trim().slice(0, 80); const range = parseFinanceDateRange(params.from, params.to); const rawMethod = first(params.method); const method = EXPENSE_METHODS.includes(rawMethod as never) ? rawMethod as (typeof EXPENSE_METHODS)[number] : "all"; const creatorId = first(params.creator) ?? "all"; const page = parsePageParam(params.page); const pageSize = parsePageSizeParam(params.pageSize); const [data, actor] = await Promise.all([getExpenses({ query, from: range.start, to: range.end, method, creatorId, page, pageSize }), getCurrentActor()]);
  const persistent = { q: query || undefined, from: range.from, to: range.to, method: method === "all" ? undefined : method, creator: creatorId === "all" ? undefined : creatorId, pageSize: pageSize === DATA_PAGE_SIZE ? undefined : String(pageSize) };
  return <><PageHeader title="Pengeluaran" description="Catat dan lacak biaya operasional yang digunakan." action={<ExpenseForm />} /><section className="flex min-w-0 flex-col overflow-hidden rounded-lg border bg-card"><form className="flex flex-col gap-3 border-b p-4 lg:flex-row lg:items-end"><DebouncedSearchInput key={query} initialValue={query} pathname="/keuangan/pengeluaran" params={persistent} placeholder="Cari keperluan pengeluaran..." ariaLabel="Cari pengeluaran" className="lg:max-w-md" /><Field className="w-full sm:w-40"><FieldLabel htmlFor="expense-from">Dari tanggal</FieldLabel><Input id="expense-from" name="from" type="date" defaultValue={range.from} /></Field><Field className="w-full sm:w-40"><FieldLabel htmlFor="expense-to">Sampai tanggal</FieldLabel><Input id="expense-to" name="to" type="date" defaultValue={range.to} /></Field><Field className="w-full sm:w-36"><FieldLabel htmlFor="expense-method-filter">Metode</FieldLabel><NativeSelect id="expense-method-filter" name="method" defaultValue={method} className="w-full"><NativeSelectOption value="all">Semua</NativeSelectOption>{EXPENSE_METHODS.map((value) => <NativeSelectOption key={value} value={value}>{EXPENSE_METHOD_LABEL[value]}</NativeSelectOption>)}</NativeSelect></Field><Field className="w-full sm:w-44"><FieldLabel htmlFor="expense-creator">Pembuat</FieldLabel><NativeSelect id="expense-creator" name="creator" defaultValue={creatorId} className="w-full"><NativeSelectOption value="all">Semua pengguna</NativeSelectOption>{data.creators.map((creator) => <NativeSelectOption key={creator.id} value={creator.id}>{creator.name}</NativeSelectOption>)}</NativeSelect></Field><Button type="submit" variant="outline">Terapkan</Button><Button variant="secondary" render={<Link href="/keuangan/pengeluaran" />} nativeButton={false}>Reset</Button></form>{data.items.length ? <div className="flex min-h-112 flex-1 flex-col"><Table className="min-w-5xl" containerClassName="min-h-0 flex-1 overflow-auto"><TableHeader className="sticky top-0 bg-muted"><TableRow className="hover:bg-muted"><TableHead>Tanggal</TableHead><TableHead>Keperluan</TableHead><TableHead>Metode</TableHead><TableHead>Dicatat oleh</TableHead><TableHead className="text-right">Nominal</TableHead><TableHead>Status Pribadi</TableHead><TableHead className="w-48 text-right">Aksi</TableHead></TableRow></TableHeader><TableBody>{data.items.map((item) => { const own = item.createdById === actor?.id; const personal = item.paymentMethod === "PRIBADI"; return <TableRow key={item.id}><TableCell>{formatDate(item.spentAt)}</TableCell><TableCell>{item.purpose}</TableCell><TableCell>{EXPENSE_METHOD_LABEL[item.paymentMethod]}</TableCell><TableCell>{item.createdBy.name}</TableCell><TableCell className="text-right font-mono tabular-nums">{formatCurrency(item.amount)}</TableCell><TableCell>{personal ? item.reimbursedAt ? <span className="text-success">Diganti {formatDate(item.reimbursedAt, true)}</span> : "Belum diganti" : "-"}</TableCell><TableCell className="text-right">{own && !item.reimbursedAt ? <div className="flex justify-end gap-2">{personal ? <form action={reimburseExpenseAction}><input type="hidden" name="id" value={item.id} /><ConfirmSubmitButton size="sm" variant="outline" pendingLabel="Memproses..." confirmTitle="Tandai uang sudah diganti?" confirmDescription="Pengeluaran ini tidak dapat diedit atau dihapus setelah ditandai." confirmLabel="Ya, sudah diganti"><Check data-icon="inline-start" aria-hidden="true" />Sudah diganti</ConfirmSubmitButton></form> : null}<ExpenseForm expense={item} /><form action={deleteExpenseAction}><input type="hidden" name="id" value={item.id} /><ConfirmSubmitButton size="sm" variant="destructive" pendingLabel="Menghapus..." confirmTitle="Hapus pengeluaran?" confirmDescription="Catatan pengeluaran ini akan dihapus." confirmLabel="Ya, hapus"><Trash2 data-icon="inline-start" aria-hidden="true" />Hapus</ConfirmSubmitButton></form></div> : "-"}</TableCell></TableRow>; })}</TableBody></Table><DataPagination pathname="/keuangan/pengeluaran" page={page} pageCount={data.pageCount} total={data.total} pageSize={pageSize} pageSizeOptions={DATA_PAGE_SIZES} params={persistent} className="border-t px-4 py-3" /></div> : <Empty className="p-12"><EmptyHeader><EmptyMedia variant="icon"><ReceiptText aria-hidden="true" /></EmptyMedia><EmptyTitle>Belum ada pengeluaran</EmptyTitle><EmptyDescription>Tambahkan catatan biaya melalui tombol Pengeluaran.</EmptyDescription></EmptyHeader></Empty>}</section></>;
}
