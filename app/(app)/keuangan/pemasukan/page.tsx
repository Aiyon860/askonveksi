import Link from "next/link";
import { Download, ReceiptText } from "lucide-react";

import { DataPagination } from "@/components/data-pagination";
import { DebouncedSearchInput } from "@/components/debounced-search-input";
import { PageHeader } from "@/components/page-header";
import { Button } from "@/components/ui/button";
import { Empty, EmptyDescription, EmptyHeader, EmptyMedia, EmptyTitle } from "@/components/ui/empty";
import { Field, FieldLabel } from "@/components/ui/field";
import { Input } from "@/components/ui/input";
import { NativeSelect, NativeSelectOption } from "@/components/ui/native-select";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { formatCurrency, formatPercentage } from "@/lib/crm/format";
import { getIncome } from "@/lib/finance/expense";
import { parseFinanceDateRange } from "@/lib/finance/date-range";
import { DATA_PAGE_SIZE, DATA_PAGE_SIZES, parsePageParam, parsePageSizeParam } from "@/lib/pagination";

type SearchParams = Promise<{ q?: string | string[]; from?: string | string[]; to?: string | string[]; status?: string | string[]; page?: string | string[]; pageSize?: string | string[] }>;
const first = (value: string | string[] | undefined) => Array.isArray(value) ? value[0] : value;

export default async function IncomePage({ searchParams }: { searchParams: SearchParams }) {
  const params = await searchParams; const query = (first(params.q) ?? "").trim().slice(0, 80); const range = parseFinanceDateRange(params.from, params.to); const status = first(params.status) === "DP" ? "DP" : first(params.status) === "LUNAS" ? "LUNAS" : "all"; const page = parsePageParam(params.page); const pageSize = parsePageSizeParam(params.pageSize);
  const state = { query, from: range.start, to: range.end, status, page, pageSize } as const; const data = await getIncome(state);
  const persistent = { q: query || undefined, from: range.from, to: range.to, status: status === "all" ? undefined : status, pageSize: pageSize === DATA_PAGE_SIZE ? undefined : String(pageSize) };
  const exportParams = new URLSearchParams(Object.entries(persistent).filter((entry): entry is [string, string] => Boolean(entry[1])));
  return <><PageHeader title="Pemasukan" description="Pantau pembayaran Invoice, HPP, laba, dan sisa pembayaran order customer." action={<Button variant="outline" render={<a href={`/api/keuangan/pemasukan/export?${exportParams}`} />} nativeButton={false}><Download data-icon="inline-start" aria-hidden="true" />Export Excel</Button>} /><section className="flex min-w-0 flex-col overflow-hidden rounded-lg border bg-card"><form className="flex flex-col gap-3 border-b p-4 lg:flex-row lg:items-end"><DebouncedSearchInput key={query} initialValue={query} pathname="/keuangan/pemasukan" params={persistent} placeholder="Cari customer, invoice, atau order..." ariaLabel="Cari pemasukan" className="lg:max-w-md" /><Field className="w-full sm:w-40"><FieldLabel htmlFor="income-from">Dari tanggal</FieldLabel><Input id="income-from" name="from" type="date" defaultValue={range.from} /></Field><Field className="w-full sm:w-40"><FieldLabel htmlFor="income-to">Sampai tanggal</FieldLabel><Input id="income-to" name="to" type="date" defaultValue={range.to} /></Field><Field className="w-full sm:w-36"><FieldLabel htmlFor="income-status">Pembayaran</FieldLabel><NativeSelect id="income-status" name="status" defaultValue={status} className="w-full"><NativeSelectOption value="all">Semua</NativeSelectOption><NativeSelectOption value="DP">DP</NativeSelectOption><NativeSelectOption value="LUNAS">Lunas</NativeSelectOption></NativeSelect></Field><Button type="submit" variant="outline">Terapkan</Button><Button variant="secondary" render={<Link href="/keuangan/pemasukan" />} nativeButton={false}>Reset</Button><p className="text-xs text-muted-foreground lg:ml-auto"><strong className="font-medium text-foreground">{data.total}</strong> invoice berbayar</p></form>{data.items.length ? <div className="flex min-h-112 flex-1 flex-col"><Table className="min-w-[110rem]" containerClassName="min-h-0 flex-1 overflow-auto"><TableHeader className="sticky top-0 bg-muted"><TableRow className="hover:bg-muted"><TableHead>Customer</TableHead><TableHead>Nama order</TableHead><TableHead>Jenis busana</TableHead><TableHead className="text-right">Total QTY</TableHead><TableHead className="text-right">Total HPP</TableHead><TableHead className="text-right">Diskon</TableHead><TableHead className="text-right">Keuntungan</TableHead><TableHead className="text-right">Laba Bersih</TableHead><TableHead className="text-right">Margin</TableHead><TableHead className="text-right">DP</TableHead><TableHead className="text-right">Lunas</TableHead><TableHead>Keterangan</TableHead></TableRow></TableHeader><TableBody>{data.items.map((item) => <TableRow key={item.id}><TableCell>{item.customer}</TableCell><TableCell>{item.orderName}</TableCell><TableCell>{item.garmentType}</TableCell><TableCell className="text-right font-mono tabular-nums">{item.quantity}</TableCell><TableCell className="text-right font-mono tabular-nums">{formatCurrency(item.hpp)}</TableCell><TableCell className="text-right font-mono tabular-nums">{formatCurrency(item.discount)}</TableCell><TableCell className="text-right font-mono tabular-nums">{formatCurrency(item.profit)}</TableCell><TableCell className="text-right font-mono tabular-nums">{formatCurrency(item.netProfit)}</TableCell><TableCell className="text-right font-mono tabular-nums">{formatPercentage(item.margin)}</TableCell><TableCell className="text-right font-mono tabular-nums">{item.dp ? formatCurrency(item.dp) : "-"}</TableCell><TableCell className="text-right font-mono tabular-nums">{item.settled ? formatCurrency(item.settled) : "-"}</TableCell><TableCell>{item.remaining === "0" ? "Lunas" : `Sisa ${formatCurrency(item.remaining)}`}</TableCell></TableRow>)}</TableBody></Table><DataPagination pathname="/keuangan/pemasukan" page={page} pageCount={data.pageCount} total={data.total} pageSize={pageSize} pageSizeOptions={DATA_PAGE_SIZES} params={persistent} className="border-t px-4 py-3" /></div> : <Empty className="p-12"><EmptyHeader><EmptyMedia variant="icon"><ReceiptText aria-hidden="true" /></EmptyMedia><EmptyTitle>Belum ada pemasukan</EmptyTitle><EmptyDescription>Invoice dengan pembayaran aktif akan tampil di sini.</EmptyDescription></EmptyHeader></Empty>}</section></>;
}
